{
  "license": "MIT",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npx pretty-quick --staged",
    "prepare": "husky install"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run frontend"
    }
  },
  "devDependencies": {
    "husky": "^6.0.0"
  }
}
